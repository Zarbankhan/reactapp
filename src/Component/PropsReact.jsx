import React from 'react'

const PropsReact = (props) => {
  
  return (
    
  <h3>My name is {props.name} {props.age} {props.id} {props.education} {props.gender}</h3>
  )
  
}

export default PropsReact
